# Copy to .env and fill in.

# Agent's Solana keypair (base58 private key). Must have a USDC ATA with balance.
SVM_PRIVATE_KEY=

# Optional: override endpoint (default: https://bridgenode.cc)
# BRIDGENODE_URL=https://bridgenode.cc
